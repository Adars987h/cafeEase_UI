# Copy to .env.local for local dev, and set the same key in Vercel for production.
# CRA only exposes vars prefixed with REACT_APP_, and inlines them at build time.
REACT_APP_API_BASE_URL=http://localhost:8081
